# Model learning script
# Reads a data matrix as an entry and outputs / updates the Recommender model

# Load the json lib
install.packages("rjson");
library(rjson);

# Get JSON data as input
d = fromJSON(file="./obj.json");




